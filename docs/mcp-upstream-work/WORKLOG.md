# 工作日志

按工作单元追加，记录实际完成内容和证据；当前任务状态集中在 [项目入口](README.md)，实现和测试细目分别维护在 CAPABILITIES、VALIDATION。

## 2026-09-18 — 起始规划与工作区准备（前序工作汇总）

- 查阅维护者讨论并审查参考分支，确认新实现需要沿用主线工具链及现有消费者约束。
- 从 upstream/master `5278fcab685723099e9950d56cb605807db5aa2a` 创建 `codex/mcp-upstream`；初始化 hbb_common 至 `0eb175963c950cba8d2f4f2a480f49db27b42c39`。原参考分支保持不变。
- 核对参考实现注册的 67 项工具能力，建立完整单 PR、五平台 MCP 服务端的范围与资源计划。
- 执行 Rust 1.75 的工作区清单解析，结果和范围见 V001；未执行完整构建或运行测试。
- 检查参考 rmcp 的工具链要求，发现 Rust 1.75 兼容问题；已查看的早期 SDK 包仍使用 edition 2024，依赖方案未定。
- 检查本机与主线 iOS 工程，确认 VM/SDK/runtime 待配置，模拟器需要单独 Rust/native 库路径。
- 用户确认本机 VM 轮换、已有 Android 与 Windows 真机优先，拿不到的设备和测试最后请求社区支援。
- 原实施计划、资源方案和能力清单位于 Git 工作区外；未开始新功能实现、未发布 PR。

## 2026-09-18 — 项目管理纳入开发分支

- 用户要求以文档持续管理长期工作，过程文档进入开发分支，最终整理 PR 时移除并 squash。
- 将三份既有计划迁入 `docs/mcp-upstream-work/`；建立统一入口、任务状态、67 项逐平台能力表、验证记录、决策/问题记录和收尾清单。
- 根 AGENTS 增加临时项目入口与更新要求，便于后续工作从当前状态继续；收尾时同时移除该入口。
- 原工作区外文档改为指向仓库内文档，避免两份进度同时维护。SSH 密钥及实际设备访问配置仍留在仓库外。
- 检查迁移后的 67 项能力与参考源码注册项完全一致、文档本地链接可解析及 Git 差异范围；本次仅涉及项目文档和临时 AGENTS 入口，没有修改产品代码。
- 尚未解决：Q01 依赖路线、Q02 模拟器构建、Q03 移动接入与生命周期；环境准备按需推进。缺设备保持社区补测状态。
- 下一步：按 README 的 A01/A02 开始主线构建及依赖验证，随后尽早推进 A03；当前没有进行中的构建进程。

## 2026-09-18 — 委派 Rust 1.75 MCP 依赖研究

- 用户要求将 SDK/协议研究交给子代理，主会话继续讨论其他事项，减少主会话上下文占用。
- A02 委派给 `mcp_rust175_research`：调查社区 SDK 的实际版本、MSRV、传递依赖、协议覆盖与平台边界；有希望的候选在仓库外隔离项目用 Rust 1.75 实际编译。
- 无合适 SDK 时，评估在成熟 HTTP/JSON 库上实现 MCP 适配层的范围、成本及互操作验证方案。
- 子代理只编写 `MCP-SDK-RESEARCH.md`，不修改产品代码、共享状态文档或提交 Git；完成后主代理审阅并汇总。当前没有结论，不代表依赖阻碍已解决。

## 2026-09-18 — 交换主目录与参考工作区的分支

- 用户要求当前项目目录直接开发新 PR，参考分支留在其他工作区。
- 暂停研究子代理，确认两个仓库及子模块均无未提交改动，研究报告尚未写入仓库；保留仓库外研究实验。
- 通过临时 detached HEAD 释放分支，依次切换：主目录 `/Users/laysath/Projects/rustdesk` 承载 `codex/mcp-upstream`，原独立目录 `/Users/laysath/.codex/worktrees/rustdesk-mcp-upstream/rustdesk` 承载 `codex/mcp-controller`。沿用既有 worktree 注册路径。
- 分别将 hbb_common 更新到主线的 `0eb175963c950cba8d2f4f2a480f49db27b42c39` 与参考分支的 `7e1c392c62d39c364127307cd408421dd5f8cfb0`。未重置分支历史或强制覆盖文件。
- 项目状态和仓库外旧文档的跳转改到主目录；子代理后续报告写入主目录下的 `docs/mcp-upstream-work/MCP-SDK-RESEARCH.md`，参考源码从原独立目录读取。
- 本次只调整工作区布局及项目记录；产品实现、构建和测试状态不变。

## 2026-09-18 — 将参考工作区改名为 reference

- 用户要求修正参考工作区仍带 upstream 的目录名。暂停子代理后，将整个 `rustdesk-mcp-upstream` 目录改名为 `rustdesk-mcp-reference`，包括仓库外旧文档跳转和测试公钥副本；主项目目录不变。
- 参考仓库的新路径为 `/Users/laysath/.codex/worktrees/rustdesk-mcp-reference/rustdesk`，分支仍为 `codex/mcp-controller`，提交仍为 `2e8ab16a5`。
- 修复 Git worktree 指针和子模块 core.worktree 路径，同步当前任务已有的 Codex 工作区附件路径；改名前的相关元数据已备份在仓库外。
- 核对 Git worktree 列表、参考仓库及子模块状态，并通过 Codex 附件接口重新读取确认新路径。子模块版本及分支内容未变，没有遗留旧路径别名。
- 更新项目入口及子代理参考路径，继续原有 SDK 研究，研究报告位置仍在主项目目录。

## 2026-09-18 — SDK 调查完成，委派主线 macOS 构建

- 收到 `mcp_rust175_research` 的完整报告，独立 SDK 调查及隔离实验已完成；调查发现旧 SDK 可用 Rust 1.75 编译，但尚无经过本项目约束验证的可直接采用现代 HTTP SDK。
- 主线 Hyper/Tokio/Serde 组合的本机 HTTP 往返及 Windows i686 cargo check 通过，范围和限制记为 V002。集中协议适配路线仍需产品集成与完整互操作验证。
- A01 本机检查发现旧参考分支留下的 ignored Flutter/Rust 桥接文件仍包含 automation 接口，需要备份后按主线重生成。vcpkg dry-run 报缺少主线 baseline 的 versions/baseline.json，需要核实并补齐缓存；尚未开始实际 Rust/Flutter 编译。
- 用户要求将主线构建也交给子代理。A01 委派给 `macos_baseline_build`，明确主目录、参考目录、现有缓存、工具链、已知问题和原版基线范围。
- 子代理只维护 `MACOS-BASELINE-BUILD.md`，日志及备份放仓库外或 ignored 路径；不提交 Git、不修改共享状态文档，不将环境处理或主线缺陷混入 MCP 产品改动。主会话继续用户讨论，结束后接收简短结论。

## 2026-09-18 — 用户确认自行实现 MCP 协议适配层

- 用户明确选择自行实现 MCP 协议层，记录为 D09；基础 HTTP/JSON 与异步 IO 复用主线已有兼容依赖，接入现有运行时，保留各平台工具链要求。
- 同步 README、PLAN、DECISIONS 与研究报告后续决策说明，结束 SDK 选型阶段；后续重点是支持版本/能力声明、工作区依赖 feature、官方客户端互操作及业务/平台集成。
- 单 PR、五平台和 67 项完整能力范围保持不变。基础栈成功记录保持原验证范围，产品协议实现及完整互操作尚未完成。
- 本次仅记录已确认的路线，未修改产品代码，也未改动构建子代理的报告或隔离实验。

## 后续日志格式

每个工作单元注明日期/任务 ID、改动及原因、验证记录 ID、仍未解决的问题和下次入口。失败尝试保留结论及适用环境，避免重复尝试。重要状态更新随对应代码提交保存。
